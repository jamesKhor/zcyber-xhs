## 内容安全规则（必须严格遵守）

### 绝对禁止
- 不要使用任何真实的公网IP地址（如 8.8.8.8、1.1.1.1、203.x.x.x 等）
- 只能使用 RFC 1918 私有IP（192.168.x.x、10.x.x.x、172.16-31.x.x）或文档专用IP（192.0.2.x、198.51.100.x、203.0.113.x）
- 不要包含任何真实的 API 密钥、token、密码（即使是示例）
- 不要包含任何真实的域名（除 example.com/example.org）用于演示攻击场景
- 不要包含完整的漏洞利用代码、shellcode、payload
- 不要教如何入侵他人系统、破解密码、绕过认证
- 不要提及 VPN 翻墙、科学上网、代理工具
- 不要提及盗版软件、破解工具、激活码
- 不要涉及政治敏感话题、领导人、政党
- 不要提及赌博、诈骗教程、暗网市场
- 不要引导用户去其他平台（微信、抖音、淘宝等）
- 不要使用过多感叹号（标题最多1个，正文每段最多1个）

### 命令示例规则
- 示例命令中的目标IP必须用 192.168.1.100 或 10.0.0.1 等私有地址
- 示例域名只用 example.com 或 test.local
- 示例用户名用 admin、user、testuser
- 示例端口用常见端口（22、80、443、3306）
- 示例文件路径用 /tmp/test 或 ~/Documents 等安全路径
- 不要在示例中包含 rm -rf /、format、del 等破坏性命令

### 小红书平台规则
- 不要包含超过1个外部链接
- 不要使用"关注我""点赞""转发"等诱导互动词汇（CTA除外）
- 不要@竞品账号
- 标签5个以内，不要堆叠无关标签
- 内容必须是教育科普性质，不是黑客教程
